<Window x:Class="TwitchChatOverlay.ChannelInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Set Twitch Channel" 
        Height="180" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FF2D2D30"
        WindowStyle="None"
        AllowsTransparency="True">
    
    <Border Background="#FF2D2D30" 
            BorderBrush="#FF6441A5" 
            BorderThickness="2"
            CornerRadius="5">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Title -->
            <TextBlock Grid.Row="0"
                       Text="Enter Twitch Channel Name"
                       Foreground="White"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Margin="0,0,0,15"/>
            
            <!-- Input -->
            <TextBox Grid.Row="1"
                     x:Name="TxtChannel"
                     Height="35"
                     Padding="10,8"
                     FontSize="14"
                     Background="#FF3F3F46"
                     Foreground="White"
                     BorderBrush="#FF555555"
                     BorderThickness="1"
                     VerticalContentAlignment="Center"
                     Margin="0,0,0,20"/>
            
            <!-- Buttons -->
            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="BtnOK"
                        Content="OK"
                        Width="80"
                        Height="32"
                        Margin="0,0,10,0"
                        Click="BtnOK_Click"
                        IsDefault="True"
                        Background="#FF6441A5"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="13"
                        Cursor="Hand"/>
                <Button x:Name="BtnCancel"
                        Content="Cancel"
                        Width="80"
                        Height="32"
                        Click="BtnCancel_Click"
                        IsCancel="True"
                        Background="#FF3F3F46"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="13"
                        Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
